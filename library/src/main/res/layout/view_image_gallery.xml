<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/image_gallery_view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/image_gallery_tab_layout"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@android:color/transparent"
        app:tabPaddingStart="10dp"
        app:tabPaddingEnd="10dp"
        app:tabBackground="@drawable/default_dot"
        app:tabIndicator="@drawable/selected_dot"
        app:tabIndicatorColor="@android:color/darker_gray"
        app:tabIndicatorGravity="stretch"
        app:layout_constraintBottom_toBottomOf="@+id/image_gallery_view_pager"/>

</RelativeLayout>